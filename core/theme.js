import { DefaultTheme } from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#065F46',
        secondary: '#0f783c',
        error: '#f13a59',
        gray: '#6b7280',
    },
};